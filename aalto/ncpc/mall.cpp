#include <iostream>
#include <unordered_map>
#include <vector>
#include <stack>
#include <algorithm> // find
#define umivi unordered_map<int, vector<int>>

using namespace std;

int main() {
    long long a, b;
    while (cin >> a >> b){
        // solve
    }
    return 0;
}