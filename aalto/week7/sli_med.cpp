#include<bits/stdc++.h>
using namespace std;


int main(){
    int n, k;
    int vals[n];
    cin >> n >> k;
    
    for (int i = 0; i < n; i++){
        cin >> vals[i];
    }
    t
    for (int i = 0; i < n; i+=k)
    {
        
    }
    
    
    vector<int> smaller(), bigger;


    return 0;
}